# Direct dependencies for the FW / nonlinear-discrimination study.
# These are the four packages the code imports directly:
#   numpy, scipy        -- core numerics, RNG (np.random.default_rng), spatial/stats
#   scikit-learn        -- classifier + cross-validated AUC
#   arch                -- GJR-GARCH / EGARCH fitting and simulation (GARCH null)
# pandas and statsmodels are pulled in transitively by `arch`.
#
# These floors are for getting the code RUNNING. They do NOT guarantee the exact
# published numbers: the GARCH simulation stream depends on the `arch` version, so
# EXACT reproduction requires the pinned closure in requirements_frozen.txt, which
# is captured from the machine that generated the canonical run. Install that file
# (into a clean virtual environment) to reproduce results to the digit.

numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
arch>=8.0
